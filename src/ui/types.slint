export struct ImpactViewData {
    number: int,
    image-x: float,
    image-y: float,
    target-x: float,
    target-y: float,
}

export struct CameraDeviceViewData {
    index: int,
    name: string,
}
